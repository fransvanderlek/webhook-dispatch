{
    "name": "webhook-dispatcher",
    "description": "dispatches webhook events to handlers",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}